# Build output
/build/

# CMake / clangd caches
/.cache/
compile_commands.json

# Editor / agent local state
/.claude/
*.swp

# Kbuild leftovers (should only ever appear under build/, but be safe)
*.o
*.ko
*.mod
*.mod.c
*.cmd
modules.order
Module.symvers
